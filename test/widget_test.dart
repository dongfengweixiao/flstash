import 'package:flutter_test/flutter_test.dart';

// TODO(flstash): replace with real widget tests once the app shell is stable.
void main() {
  test('placeholder test', () {
    expect(true, isTrue);
  });
}
