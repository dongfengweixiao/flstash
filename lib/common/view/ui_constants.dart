import 'package:flutter/material.dart';

const kLargestSpace = 20.0;
const kMediumSpace = 10.0;
const kSmallestSpace = 5.0;

const kMasterDetailBreakPoint = 720.0;
const kAdaptivContainerBreakPoint = 1200.0;

const kSideNavWidth = 240.0;
const kCompactSideNavWidth = 64.0;

/// ThemeTile 迷你窗口预览中 dark 模式的底色。
const kDarkPreviewColor = Color(0xFF2D2D2D);

/// 默认强调色（占位，后续按 flstash 品牌色调整）。
const kFlstashDefaultColor = Color(0xFF8E24AA);
